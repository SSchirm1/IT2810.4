import { Icon } from "@chakra-ui/core";
import styled from "@emotion/styled";
import { StarIcon } from "@chakra-ui/icons";

export const StyledIcon = styled(StarIcon)`
  hover: pointer;
  margin: 3px;
`;
